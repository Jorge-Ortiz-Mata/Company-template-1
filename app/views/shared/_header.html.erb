<nav class="d-flex flex-coulmn justify-content-between navMenu">
    <div class="mt-auto mb-auto ml-2">
        <%= link_to "Company Inc.", root_path, class:"navbar-brand  text-white h3" %>
    </div>
    <div class="mt-auto mb-auto">
        <ul class="d-flex flex-row sign mt-auto mb-auto">
            <% if !current_user %>
                <li class="mr-2 mt-auto mb-auto">
                    <%= link_to "Sign in", new_user_session_path, class:"btn btn-info" %>
                </li>
                <li class="mr-2 mt-auto mb-auto">
                    <%= link_to "Create account", new_user_registration_path, class:"btn btn-success" %>
                </li>
            <% else %>
                <li  class="mr-2 mt-auto mb-auto">
                    <%= "#{current_user.email}" %>
                </li>
                <li class="mr-2 mt-auto mb-auto">
                    <%= link_to "Sign out", destroy_user_session_path, method: :delete, class:"btn btn-danger" %>
                </li>
            <% end %>
        </ul>
    </div>
</nav>